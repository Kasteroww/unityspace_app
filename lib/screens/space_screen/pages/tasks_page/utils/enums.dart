enum TaskGrouping {
  byProject,
  byDate,
  byUser,
  noGroup,
}

enum TaskFilter {
  onlyActive,
  onlyCompleted,
  allTasks,
}

enum TaskSort {
  byDate,
  byStatus,
  byImportance,
  defaultSort,
}
