import 'package:flutter/material.dart';

class PopupMenuMembersActionItem extends StatelessWidget {
  const PopupMenuMembersActionItem({
    required this.child,
    super.key,
  });

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
